import React from 'react';
import Banner from '../../components/Homepage/Banner';

const Homepage = () => {
    return (
        <main>
            <Banner/>
        </main>
    );
};

export default Homepage;